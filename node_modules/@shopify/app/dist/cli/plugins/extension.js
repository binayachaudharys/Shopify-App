import { plugins } from '@shopify/cli-kit';
import { getArrayRejectingUndefined } from '@shopify/cli-kit/common/array';
export const defineExtensionSpecs = (input) => {
    return async () => input;
};
export const defineFunctionSpecs = (input) => {
    return async () => input;
};
export async function getListOfExtensionSpecs(config) {
    const hooks = await plugins.fanoutHooks(config, 'extension_specs', {});
    const specs = getArrayRejectingUndefined(Object.values(hooks)).flat();
    return specs;
}
export async function getListOfFunctionSpecs(config) {
    const hooks = await plugins.fanoutHooks(config, 'function_specs', {});
    const specs = getArrayRejectingUndefined(Object.values(hooks)).flat();
    return specs;
}
//# sourceMappingURL=extension.js.map