"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useShippingAddress = useShippingAddress;

var _errors = require("../errors");

var _api = require("./api");

var _subscription = require("./subscription");

/**
 * Returns the proposed `shippingAddress` applied to the checkout.
 */
function useShippingAddress() {
  const shippingAddress = (0, _api.useExtensionApi)().shippingAddress;

  if (!shippingAddress) {
    throw new _errors.ScopeNotGrantedError('Using shipping address requires having shipping address permissions granted to your app.');
  }

  return (0, _subscription.useSubscription)(shippingAddress);
}