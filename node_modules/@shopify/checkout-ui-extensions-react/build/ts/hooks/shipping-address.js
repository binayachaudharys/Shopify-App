"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useShippingAddress = void 0;
const errors_1 = require("../errors");
const api_1 = require("./api");
const subscription_1 = require("./subscription");
/**
 * Returns the proposed `shippingAddress` applied to the checkout.
 */
function useShippingAddress() {
    const shippingAddress = (0, api_1.useExtensionApi)().shippingAddress;
    if (!shippingAddress) {
        throw new errors_1.ScopeNotGrantedError('Using shipping address requires having shipping address permissions granted to your app.');
    }
    return (0, subscription_1.useSubscription)(shippingAddress);
}
exports.useShippingAddress = useShippingAddress;
